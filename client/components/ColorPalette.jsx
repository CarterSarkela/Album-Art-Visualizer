import React from "react";

const ColorPalette = () => {
  const colors = ['Black', 'Blue', 'Green', 'Yellow', 'White'] 
  return(
    <div>
      {/* {colors.map((el, index) => (
        <div>{el + index}</div>
      ))} */}
    </div>
  )
}

export default ColorPalette;